class Enemy {
    private var hpCurrent: Int
    private var mpCurrent: Int
    private val hpMax = 100
    private val mpMax = 50
    private var atk = 5

    init {
        hpCurrent = hpMax
        mpCurrent = hpMax
    }

}